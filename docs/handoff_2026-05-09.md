# Handoff — 2026-05-09 evening

Single-page snapshot Raghu can read post-compaction to pick up the thread, and the next agent can use to ground itself.

## What just shipped today

- **Synexar corpus ingested** as a second facility (`SYNX`, `id 33333333-3333-3333-3333-333333333333`) under the existing PracticeX tenant — 58 docs, 6.1 MB, drop at `data/drops/synexar/`.
- **Per-facility Practice Intelligence Brief** — `doc.portfolio_briefs` now keyed on `(tenant_id, facility_id)`. `Guid.Empty` = "all facilities" sentinel. Endpoint takes `?facility=<guid>`. Frontend re-fetches when the URL `?facility=` flips. Commit `130deac`.
- **Synexar / early-stage corporate taxonomy** — 9 new candidate types (`board_resolution`, `equity_grant`, `ip_assignment`, `corp_formation`, `regulatory_filing`, `privacy_policy`, `terms_of_service`, `term_sheet`, `founders_meeting`) and 6 new families (`corp_governance`, `equity`, `ip`, `corp_formation`, `regulatory`, `policy`). Commit `3fa2bf7`.
- **Two briefs currently exist**: tenant-wide (79 docs, "all facilities") and Synexar-only (53 docs, opens "Synexar, Inc. (SYNX)" with zero Eagle bleed).

## What's broken / outstanding

| Area | Status |
|---|---|
| **Tenant isolation** | Eagle + Synexar still share `tenant_id`. Insights panel, entity graph, Counsel's Brief, default tenant-wide brief, search, renewals — all mingle. Raghu greenlit the tenant-split; not started. See `memory/project_tenant_isolation_required.md` for the migration plan. |
| **Counsel's Memo (Slice 20)** | Failing on the Synexar BOI transcript with "Counsel memo generation failed". Likely chokes on short structured filing data. Needs investigation in `LegalMemoEndpoint` (or wherever Stage A runs). |
| **3 Synexar bad-JSON LLM** | `05_IP_Assignment_Agreement.docx`, `24_Advisory_Board_Agreement.docx`, `36_Channel_Partner_Agreement.docx` — LLM produced unescaped quotes; `db.SaveChangesAsync` rejected. Need a sanitizer + try/catch around the save in `LlmExtractionEndpoint.cs:139` so a single bad doc doesn't kill the batch. |
| **1 SSL transient** | `37_Board_Resolution_CEO_Appointment_Apr2026 (1).docx` — retry will likely succeed. |
| **Storage path drift** | Eagle's 27 blobs landed under `src/PracticeX.Api/var/practicex/documents/...`; Synexar's 57 under `src/PracticeX.Api/bin/Debug/net9.0/var/practicex/documents/...`. Both are reachable but `appsettings.Development.json` has the deeper Debug path baked in. Worth normalizing. |
| **Eagle-only brief not yet generated** | When the Eagle pill is selected the brief is the tenant-wide one. `POST /api/analysis/portfolio-brief?facility=22222222-2222-2222-2222-222222222222` mints an Eagle-scoped one. |

## Decisions Raghu made or affirmed

- **Chatbot + MCP** — yes to both for premium tier, build chatbot first, MCP second. Both queue *behind* the tenant-split work.
- **Facility-as-first-class structural lift** — paused; per-facility brief shipped as the first concrete step. Next: scope the rest of Insights / Entity Graph / Counsel's Brief.
- **Hard isolation between Eagle and Synexar** — no information should ever cross. Tenant-split is the load-bearing fix.

## Operational notes

- API service: `PracticeXApi` (NSSM), restarts via `sc stop/start`. Secrets at `C:/Services/PracticeX.Api/appsettings.Development.json` — preserve on every binary swap with the `/XF` exclude list.
- Frontend deploy: `bash scripts/deploy_frontend.sh` (must run with cwd inside `apps/command-center/` — script enforces it).
- Production URL: `https://app.practicex.ai` (Cloudflare Access OTP gate, 30-day session, Parag whitelisted at `dr8382@gmail.com`).
- Cloudflared tunnel: `cloudflared` Windows service. Both services are RUNNING / AUTO_START.
- DB: PostgreSQL 18, port 5436, password `rv5791`. `psql` at `C:/Program Files/PostgreSQL/18/bin/psql.exe`.
- `MaintenanceMessage` auto-retries on 3s/6s/12s back-off; `request()` in `api.ts` auto-reloads on Cloudflare Access HTML responses.

## Next session start order

1. Read `docs/roadmap.md` for canonical "where are we" framing.
2. Read `memory/project_tenant_isolation_required.md` and `memory/project_synexar_onboarding.md`.
3. Verify production health: `curl -sk https://localhost:7100/api/system/info` should be 200.
4. Confirm with Raghu whether to proceed with the tenant-split or chase the Counsel's Memo bug first.
